- [x] Increase dither canvas resolution (160x90 → 480x270), boost BASE_ALPHA, scale SCAN_HALF_HEIGHT
- [x] Make bg-surface and bg-raised semi-transparent in globals.css @theme (rgba with 0.50-0.55 alpha)
- [x] Add global backdrop-filter blur rule for .bg-bg-surface and .bg-bg-raised classes
- [x] Update .glass-panel to use bg-void-70 with stronger blur
- [x] Remove bg-bg-void from page containers (landing page, upload page)
- [x] Make sidebar background lighter (bg-void-95 → bg-void-75)
- [x] Make header background lighter (bg-void-80 → bg-void-70)
- [x] Make login/signup auth cards lighter (bg-void-92 → bg-void-70)
- [x] Make commentary ticker and score bug lighter (bg-void-92 → bg-void-75)
- [x] Add glass effect to settings sticky header and mobile nav dropdown
- [x] Wrap layout content in relative z-1 div to stack above dither canvas
- [x] Add backdrop-blur to animated-counters inline styles
- [x] make the dither much higher resolution. if necessary, use webgl instead of <canvas> we are using now.
- [x] make the transition effects softer and less apparent
- [x] even higher dither resolution
- [x] make sure all components are not opaque (show background)
- [x] the dither transition needs to be smooth. the particles need to move around in an interesting rearranging fashion.
- [x] the thing doesn't evne run, fix it Failed to execute 'getRandomValues' on 'Crypto': The ArrayBufferView's byte length (262144) exceeds the number of bytes of entropy available via this API (65536).